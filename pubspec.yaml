name: simagang_app
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Tambahan library
  intl: ^0.19.0
  table_calendar: ^3.0.9
  fl_chart: ^0.68.0

  # (opsional) package lain yang mungkin kamu pakai
  http: ^1.1.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
